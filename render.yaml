services:
  - type: web
    name: opsweave
    runtime: docker
    plan: free
    healthCheckPath: /api/health
    envVars:
      - key: PUBLIC_ORIGIN
        fromService:
          type: web
          name: opsweave
          envVarKey: RENDER_EXTERNAL_URL
      - key: DATABASE_URL
        sync: false
